# Task: Runner State Diff Logging

When a test mutates the environment or state, we currently have no visibility. Implement optional logging (e.g., `--debug`) that records env diffs before/after each test so we can audit unexpected mutations during development.
